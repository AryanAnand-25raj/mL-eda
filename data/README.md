# Data Directory

Place local CSV datasets in `data/raw/`.

Expected files for the current notebooks:

- `Advertising.csv`
- `dm_office_sales.csv`
- `hearing_test.csv`

The raw data folder is ignored by Git to avoid uploading local or course-provided datasets accidentally. If a dataset is public and safe to share, add its source link to the main README before committing it.
